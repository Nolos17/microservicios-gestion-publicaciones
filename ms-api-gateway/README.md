ejecutamos
mvn clean install
mvn clean package

Ejecutar el JAR generado:

java -jar target/ms-api-gateway-0.0.1-SNAPSHOT.jar

mvn spring-boot:run
disponible en 

http://localhost:8000

docker-compose up --build